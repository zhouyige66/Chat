<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowNoTitle">true</item>

        <item name="android:windowAnimationStyle">@style/StyleActivityAnim</item>
    </style>

    <style name="ActivityFullScreen" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="StyleActivityAnim">
        <item name="android:activityOpenEnterAnimation">@anim/anim_right_enter</item>
        <item name="android:activityOpenExitAnimation">@anim/anim_left_exit</item>
        <item name="android:activityCloseEnterAnimation">@anim/anim_left_enter</item>
        <item name="android:activityCloseExitAnimation">@anim/anim_right_exit</item>
    </style>

    <style name="StylePopupWindowAnim">
        <item name="android:windowEnterAnimation">@anim/anim_pop_enter</item>
        <item name="android:windowExitAnimation">@anim/anim_pop_exit</item>
    </style>
</resources>
