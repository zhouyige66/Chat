server:
  port: 8081

spring:
  profiles:
    active: service
  redis:
    host: localhost
    port: 6379
  servlet:
    multipart:
      # 单个文件大小
      max-file-size: 500MB
      # 总大小
      max-request-size: 10GB

logging:
  file:
    max-history: 10
    max-size: 10MB
  path: log/log.log

feign:
  okhttp:
    enabled: true

file:
  parentPath: /Users/zzy/Desktop/file/
